#!/usr/bin/env bash

# Load Global Variable
source $HOME/.aether-corevar

if [[ "$1" == "change" ]]; then
    urxvt -e ~/.config/aether/scripts/change-wallpaper
else
    if [[ $WALLPAPER == "DEFAULT" ]]; then
        if [[ $THEME == "aetherLight" ]]; then
            feh --bg-fill ~/.wallpaper/aether-light.png
        elif [[ $THEME == "aetherDark" ]]; then
            feh --bg-fill ~/.wallpaper/aether-dark.png
        fi
    else
        feh --bg-fill $WALLPAPER
    fi
fi
